# Prime-power congruences for odd-exponent binomial sums

For a nonnegative integer $m$, define

$$
u_m(n)=\mathrm{Num}\left(
\sum_{k=1}^{n}\frac{1}{k^{2m+1}}
\binom{n}{k}^{2}\binom{n+k}{k}^{2}
\right).
$$

Here the numerator is taken after reducing the rational number to lowest
terms. The lower endpoint is $k=1$; a $k=0$ term would be undefined.

This conjecture is recorded in [OEIS A357513](https://oeis.org/A357513);
the OEIS entry states the parameterized version treated here.

The mathematical result proved here is the exact classification

$$
p^4\mid u_m(p-1)
\quad\Longleftrightarrow\quad
p-1\nmid 2m+4\ \text{or}\ p\mid 2m+7.
$$

Equivalently, the exceptional set is

$$
\mathcal E_m=
\{p\text{ prime}:p-1\mid2m+4\text{ and }p\nmid2m+7\}.
$$

It is finite: every $p\in\mathcal E_m$ satisfies $p\le 2m+5$.
At an exceptional prime, the reduced numerator has valuation $2$ when
$p=2$, and valuation $3$ when $p$ is odd.

## Proof outline

Write

$$
S_{m,p}=\sum_{k=1}^{p-1}\frac{1}{k^{2m+1}}
\binom{p-1}{k}^{2}\binom{p-1+k}{k}^{2}.
$$

### 1. Binomial-product reduction

The exact product factorization reduces each summand modulo $p^4$, giving

$$
S_{m,p}\equiv p^2H_{2m+3}-2p^3H_{2m+4}\pmod{p^4}.
$$

### 2. Harmonic pairing

Pairing $k$ with $p-k$ yields

$$
2H_{2m+3}+(2m+3)pH_{2m+4}\equiv0\pmod{p^2},
$$

and hence

$$
2S_{m,p}\equiv-(2m+7)p^3H_{2m+4}\pmod{p^4}.
$$

### 3. Finite-field power sum

Modulo $p$,

$$
H_{2m+4}\equiv
\begin{cases}
0,&p-1\nmid2m+4,\\
-1,&p-1\mid2m+4.
\end{cases}
$$

This gives the exact exceptional-prime criterion above.

### 4. Reduced numerator

The common denominator $D_m=((p-1)!)^{2m+1}$ is coprime to $p$.
Therefore the modular congruence for $S_{m,p}$ transfers to its reduced
numerator.

## Contents

- [proof.tex](proof.tex) — the generalized mathematical proof, including
  the exact exceptional set and exceptional valuations.
- [Proof.lean](Proof.lean) — the Lean 4 + Mathlib formalization of the
  generalized divisibility theorem. It contains no placeholders or added
  axioms.

The formalized exceptional set has the exact membership characterization:

```lean
def exceptionalPrimes (m : ℕ) : Finset ℕ :=
  (Finset.range (2 * m + 6)).filter fun p =>
    p.Prime ∧ p - 1 ∣ 2 * m + 4 ∧ ¬ p ∣ 2 * m + 7
```

The principal formalized theorem is:

```lean
theorem u_prime_sub_one_dvd_of_not_mem
    {m p : ℕ} [Fact p.Prime] (hpnot : p ∉ exceptionalPrimes m) :
    ((p ^ 4 : ℕ) : ℤ) ∣ u m (p - 1)
```

There is also the equivalent direct sufficient-condition theorem
**u_prime_sub_one_dvd_of_good**.

## Acknowledgment

The mathematical proof and Lean formalization in this repository were
generated by **GPT-5.6 Sol**.

## Verification

The project uses Lean **v4.29.0** and Mathlib **v4.29.0**.

```text
lake exe cache get
lake build
```

The build checks the complete proof with the Lean kernel.
